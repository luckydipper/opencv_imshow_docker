cmake_minimum_required(VERSION 3.1)
project(docker_imshow)
find_package( OpenCV REQUIRED)
include_directories(${OpenCV_INCLUDE_DIRS})
add_executable(imshow_img.o imshow_img.cpp)
target_link_libraries(imshow_img.o ${OpenCV_LIBS})